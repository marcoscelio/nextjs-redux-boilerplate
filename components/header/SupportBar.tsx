import React from "react";

import styles from "../../styles/SupportBar.module.css";

const SupportBar = (props: any) => {
  return <div className={styles.Container}>Suporte bar</div>;
};

export default SupportBar;
