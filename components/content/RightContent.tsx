import React from "react";

import styles from "../../styles/RightContent.module.css";

const RightContent = (props: any) => {
  return <div className={styles.Container}>RightContent</div>;
};

export default RightContent;
